--name          "Tribler"
--version       0.1
--android_api   16
--package       org.tribler.android
--private       ./app
--arch          armeabi
--dist_name     testproject
--bootstrap     sdl2
--requirements  cherrypy,libnacl,requests,python2,numpy,kivy,openssl,twisted,pyasn1,pil,netifaces,libsodium,libtorrent
--permission    WRITE_EXTERNAL_STORAGE
--permission    INTERNET
